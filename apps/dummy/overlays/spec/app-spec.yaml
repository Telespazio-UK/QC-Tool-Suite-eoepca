project: default
source:
  repoURL: 'https://eoepca.github.io/helm-charts-dev/'
  targetRevision: 1.0.1
  helm:
    releaseName: dummy-service2
    values: |
      ingress:
        enabled: true
        annotations:
          kubernetes.io/ingress.class: nginx
          ingress.kubernetes.io/ssl-redirect: "false"
          nginx.ingress.kubernetes.io/ssl-redirect: "false"
          cert-manager.io/cluster-issuer: letsencrypt
        hosts:
          - host: dummy-service-open2.185-52-193-246.nip.io
            paths:
              - path: /
                pathType: ImplementationSpecific
        tls:
          - hosts:
              - dummy-service-open2.185-52-193-246.nip.io
            secretName: dummy-service-open2-tls
  chart: dummy
destination:
  server: 'https://kubernetes.default.svc'
  namespace: test
syncPolicy:
  automated:
    prune: true
    selfHeal: true
    allowEmpty: true
  syncOptions:
    - Validate=true
    - CreateNamespace=true
    - PrunePropagationPolicy=foreground
    - PruneLast=true
